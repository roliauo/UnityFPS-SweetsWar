using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Game.SweetsWar
{
    public class Test : MonoBehaviour
    {
        /*
        public List<int> ItemList = new List<int>(5);
        void Start()
        {
            Debug.LogFormat("list size: {0}, {1}", ItemList.Capacity, ItemList.Count);
            for (int i = 0; i < 6; i++)
            {
                ItemList.Add(i);
            }
            
            Debug.LogFormat("list size: {0}, {1}", ItemList.Capacity, ItemList.Count);
            for (int i = 0; i < ItemList.Capacity; i++)
            {
                Debug.Log("item: " + ItemList[i]);
            }
        }
        */

    }
}

